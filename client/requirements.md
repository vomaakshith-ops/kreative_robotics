## Packages
framer-motion | Complex animations for hero section and scroll reveals
lucide-react | Icons for the UI (already in base, but emphasizing usage)
react-hook-form | Form handling for contact/inquiry forms
zod | Schema validation for forms (already in base)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Plus Jakarta Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
